version: 2
service: waldo

artifacts:
  cli:
    entrypoint: cmd/waldo/main.go

deployments:
  application:
    artifact: cli
    from:
      adapter: facts
      source: testdata/deployment/local.yaml
      resource: application

dispositions: []
