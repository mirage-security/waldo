version: 2
service: fixture

artifacts:
  worker:
    entrypoint: src/checkout/expiry.example

deployments:
  worker:
    artifact: worker
    from:
      adapter: facts
      source: deployment/worker.yaml
      resource: worker

dispositions: []
